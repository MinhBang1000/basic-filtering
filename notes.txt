# Chú ý:
- Học lại về các độ đo
    - Trung bình mean
    - Độ lệch chuẩn
    - Phân phối Gaussian
    - inflate
    - AUC
- Coi các giải thuật lại
- Test bằng một SOTA method

1. Các giải pháp mới cho layer 1
- Perplexity
- Semantic Divergence (Độ lệch ngữ nghĩa) qua Sliding Window: Nếu Cosine Similarity giảm đột ngột (ví dụ từ 0.8 xuống 0.2), đó là dấu hiệu của một sự chuyển đổi chủ đề (Topic Shift) không mong muốn.
- Topic Drift Score: LDA (Latent Dirichlet Allocation) hoặc BERTopic.
- Instruction Density & Imperative Ratio (Mật độ câu lệnh)
- Cross-Attention Entropy (Độ tự do của sự chú ý)
- Yếu tố kết hợp:
    - PPL Spike
    - Semantic Divergence
    - Imperative Ratio
    => Dùng chúng như các features cho Random Forest


2. Optimization:
- Xử lý dữ liệu
- Data Augementation
    - GCG 
        -> Thay đổi temperature để các target-string khác nhau
        -> Quá chậm không thể generate cho toàn tập dolly 15k được
    - AutoDAN: Dựa trên benign mà generate nhanh hơn -> Nhược điểm là chỉ có direct prompt injection
    - Rewrite-to-Jailbreak (R2J): Nhìn jailbreak là paraphrasing problem
    - Template: DONE
    - EasyJailBreak
    - Host another LLM that can refactor Dolly dataset follow our Agent.
- Khả năng tự thích ứng

3. Concrete Plan
Giải pháp của tôi là một hệ thống phòng thủ 3 lớp (3-layer defense) thiết lập cơ chế Zero Trust tại cửa ngõ đầu vào của AI Agent để ngăn chặn tấn công injection 
đa nguồn (User, Tool Outputs, Memory). Lớp thứ nhất sử dụng mô hình phát hiện bất thường (Anomaly Detection) được huấn luyện hoàn toàn trên dữ liệu lành tính (benign data) 
đặc thù của doanh nghiệp để phân loại nhanh đầu vào thành hai nhóm: Bình thường (Normal) hoặc Nghi ngờ (Suspicious). Lớp thứ hai áp dụng các bộ quy tắc (Rule-based) 
hoặc mô hình AI siêu nhẹ để nhận diện và loại bỏ tức thì các mẫu tấn công đã biết (well-known attacks) bên trong nhóm Nghi ngờ. Lớp cuối cùng sử dụng mô hình Transformer 
hoặc LLM để thẩm định chuyên sâu, giúp phân biệt chính xác giữa dữ liệu lành tính mới (zero-day benign) và mã độc mới (zero-day malicious) trong số các mẫu nghi ngờ còn lại, 
từ đó giảm tối đa tỷ lệ báo động giả (FPR) và đảm bảo an toàn tuyệt đối trước khi dữ liệu được Agent xử lý.

