# Đảm bảo đã export HF_TOKEN nếu cần
python -m vllm.entrypoints.openai.api_server \
    --model mlabonne/Meta-Llama-3.1-8B-Instruct-abliterated \
    --port 8000 \
    --dtype bfloat16 \
    --gpu-memory-utilization 0.8 \
    --max-model-len 4096 \
    --api-key EMPTY